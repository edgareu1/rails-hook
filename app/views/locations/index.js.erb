// Refresh the list of Locations
document.getElementById('cards-list').innerHTML = "<%= j render 'locations/list', locations: @locations %>";

// Add smooth animations to the pagination links
smoothPagination();
