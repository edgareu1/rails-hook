<%= simple_form_for(@location) do |f| %>
  <%= f.input :site, label: false, placeholder: "Spot's name" %>
  <%= f.input :name, label: false, placeholder: "Location" %>

  <div class="form-actions">
    <%= f.button :submit, class: "btn btn-flat btn-block" %>
  </div>
<% end %>
